local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "hcl", "terraform" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
